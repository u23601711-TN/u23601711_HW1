@model u23601711_HW1.Models.BookingVM
@{
    ViewBag.Title = "BookingForm";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
@* <head>
        <style>
                body {
                    background: #f9f9ff;
                    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                    color: #4a4a73;
                    padding: 2rem;
                }

                .form-control, .form-select {
                    background: #f8f8ff;
                    border: none;
                    border-radius: 30px;
                    padding: 0.75rem 1.5rem;
                    font-size: 0.9rem;
                    color: #4a4a73;
                }

                    .form-control::placeholder {
                        color: #b0b0cc;
                    }

                .form-select {
                    -webkit-appearance: none;
                    -moz-appearance: none;
                    appearance: none;
                    background-image: url('data:image/svg+xml;utf8,<svg fill="%234a4a73" height="24" viewBox="0 0 24 24" width="24" xmlns="http://www.w3.org/2000/svg"><path d="M7 10l5 5 5-5z"/></svg>');
                    background-repeat: no-repeat;
                    background-position: right 1.25rem center;
                    background-size: 1rem;
                    padding-right: 3rem;
                }

                .input-group-text {
                    background: transparent;
                    border: none;
                    padding-left: 0;
                    color: #4a4a73;
                    font-size: 1.2rem;
                }

                label {
                    font-size: 0.75rem;
                    font-weight: 600;
                    margin-bottom: 0.25rem;
                    opacity: 0.7;
                }

                textarea.form-control {
                    height: 80px;
                    resize: none;
                    border-radius: 15px;
                    padding-top: 0.75rem;
                    padding-left: 1.5rem;
                }

                button.btn-book {
                    background-color: #57557a;
                    color: #fff;
                    border-radius: 30px;
                    padding: 0.75rem 0;
                    font-weight: 600;
                    width: 100%;
                    font-size: 1rem;
                    transition: background-color 0.3s ease;
                }

                    button.btn-book:hover {
                        background-color: #45456a;
                    }
            </style>

    </head>*@

<h2>@Model.ServiceName</h2>


<form method="post" action="/Booking/SubmitBooking">
    <input type="hidden" name="serviceId" value="@Model.ServiceId" />

    @*<div class="row g-3">*@
    <div class="col-md-6">
        <label for="fullName" class="form-label">Full Name<span class="text-danger">*</span></label>
        <input type="text" class="form-control" id="fullName" placeholder="John Carter" required />
    </div>
    <div class="col-md-6">
        <label for="phone" class="form-label">Phone<span class="text-danger">*</span></label>
        <input type="tel" class="form-control" id="phone" placeholder="+27 00 000 0000" required />
    </div>
    <div class="mb-3">
        <label>Pick-Up Time</label>
        <input type="datetime-local" name="pickupTime" class="form-control" required />
    </div>

    <div class="col-md-6">
        <label for="reason" class="form-label">Reason<span class="text-danger">*</span></label>
        <select id="reason" class="form-select" required>
            <option value="" disabled selected>Choose reason</option>
            <option>Accident</option>
            <option>option 2</option>
            <option>option 3</option>
            <option>option 4</option>
        </select>
    </div>




    <div class="col-md-6">
        <label for="vehicle" class="form-label">Vehicle<span class="text-danger">*</span></label>
        <select id="vehicle" class="form-select" required>
            <option value="" disabled selected>Choose vehicle</option>
            @foreach (var vehicle in Model.Vehicles)
            {
                <option value="@vehicle.VehicleID">@vehicle.VehicleType</option>
            }


        </select>
    </div>

    <div class="col-md-6">
        <label for="driver" class="form-label">Driver<span class="text-danger">*</span></label>
        <select id="driver" class="form-select" required>
            <option value="" disabled selected>Choose driver</option>
            @foreach (var driver in Model.Drivers)
            {
                <option value="@driver.DriverID">@driver.DriverFirstName @driver.DriverLastName</option>
            }
        </select>
    </div>

    <div class="col-6">
        <label for="address" class="form-label">Pick-Up Address</label>
        <textarea id="address" class="form-control" placeholder="Please type your address here..."></textarea>
    </div>

    <div class="col-12">
        <button type="submit" class="btn btn-book">BOOK AMBULANCE</button>
    </div>

</form>

